* Cucumber `TestEngine`
  * supports discovery and execution of ".feature" -- via the  --
    * `ClasspathRootSelector`
    * `ClasspathResourceSelector`
    * `PackageSelector`
    * `FileSelector`
    * `DirectorySelector`
    * `UniqueIdSelector`
    * `UriSelector`
  * id = "cucumber"